# Optional Hyprland rules for waylandkb.
# Include this file from hyprland.conf after adjusting paths if needed.

# Hyprland 0.53+ rule syntax. ignore_alpha prevents the compositor from
# blurring the transparent rectangle outside the rounded GTK panel.
layerrule = blur on, ignore_alpha 0.08, xray off, match:namespace waylandkb
